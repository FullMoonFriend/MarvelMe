# Only required when running `npm run fetch-heroes` to regenerate
# src/data/heroes.json from the Superhero API. The runtime app does not
# read this — hero data is bundled at build time.
VITE_SUPERHERO_API_TOKEN=
